Streaming: sending data gradually in real time, instead of all at once
    Without streaming:

        model generates full text → send once

    With streaming:

        model generates token → send → repeat
first token appears quickly
confirms system is working